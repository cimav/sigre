<div id="solicitud-cedulas-header">
  <div class="row" id="cedulas-title">
    <div class="col-md-10">
      <h4>Cédulas de control de costos</h4>
    </div>
    <div class="col-md-2">
      <div class="pull-right cedula-reload">
        <span {{action "reloadCedula" cedula }} class="glyphicon glyphicon-refresh"></span>
      </div>
    </div>
  </div>
  <div id="cedulas-wrapper">
    <table id="tabla-datos">
      <tr>
        <td id="columna-cliente">
          <div id="cliente-nombre" class="dato-bold">{{cliente.razon_social}}</div>
          <div id="cliente-rfc">{{cliente.rfc}}</div>
          <div id="cliente-ciudad">{{cliente.ciudad}}</div>
          <div id="cliente-direccion">{{cliente.calle_num}}</div>
        </td>
        <td id="columna-contacto">
          <div id="contacto-nombre" class="dato-bold">{{contacto.nombre}}</div>
          <div id="contacto-tel">{{contacto.telefono}}</div>
          <div id="contacto-email">{{contacto.email}}</div>
        </td>
      </tr>
    </table>
  </div>

  <div id="cedulas-list">

    <div id="columna-lista">
      <ul class="nav nav-pills">
        {{#each cedula in cedulas}}
          {{#link-to 'cedula' cedula tagName="li"}}<a href="#"><span>{{cedula.codigo}}</span> </a>{{/link-to}}
        {{/each}}
      </ul>
    </div>

    <div id="columna-costos">
      <table>
        <tr>
          <td><span class="cedula-etiqueta">orden compra</span></td>
          <td><span class="cedula-costo-valor">{{orden_compra}}</span></td>
        </tr>
        <tr>
          <td><span class="cedula-etiqueta">precio venta</span></td>
          <td><span class="cedula-costo-valor">{{formatCurrency precio_venta}}</span></td>
        </tr>
        <tr>
          <td><span class="cedula-etiqueta">costo interno acumulado</span></td>
          <td><span class="cedula-costo-valor">{{formatCurrency costo_interno}}</span></td>
        </tr>
      </table>
    </div>
  </div>

</div>

<div id="cedulas-workarea">
{{outlet}}
</div>
