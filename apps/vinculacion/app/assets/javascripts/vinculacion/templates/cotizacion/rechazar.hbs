{{#form-for controller}}

    <h5>Cotización rechazada por el cliente</h5>

    {{input motivo_status as="text" label='Motivo'}}

    <div class="form-group">
        <button type="submit"
                class="btn btn-primary glyphicon glyphicon-edit" {{bind-attr disabled=this.isNotDirty}} {{action "reeditar" this target='controller'}} >
            Re-Editar
        </button>
        <button type="submit"
                class="btn btn-warning glyphicon glyphicon-usd" {{bind-attr disabled=this.isNotDirty}} {{action "recotizar" this target='controller'}} >
            Re-Cotizar
        </button>
    </div>

{{/form-for}}