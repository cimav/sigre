<h3>{{codigo}} - {{nombre}}</h3>

{{#form-for controller}}

  {{input codigo label='Código'}}
  {{input nombre }}

  <div class="form-group">
      <div class="col-sm-offset-2 col-sm-6">
          <button type="submit"
                  class="btn btn-primary" {{bind-attr disabled=this.isNotReadyForSave}} {{action "update" this target='controller'}} >
            {{#if item.isSaving}}
                <span class="spin glyphicon glyphicon-refresh"></span>
                Guardando...
            {{else}}
                Guardar
            {{/if}}
          </button>
      </div>
      <div class="col-sm-1">
          <button class="btn-delete btn btn-danger" {{bind-attr disabled=this.hasSolicitudes}} {{action "destroy" this target='controller'}}><span title="Eliminar proyecto" class="glyphicon glyphicon-trash"></span></button>
      </div>
  </div>

{{/form-for}}

